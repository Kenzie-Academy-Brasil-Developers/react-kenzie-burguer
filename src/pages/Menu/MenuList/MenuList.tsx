import { MenuCard } from "./MenuCard";
import { StyledMenuList } from "./StyledMenuList";

export function MenuList() {
  return (
    <StyledMenuList>
        <MenuCard/>
        <MenuCard/>
        <MenuCard/>
        <MenuCard/>
    </StyledMenuList>
  )
}